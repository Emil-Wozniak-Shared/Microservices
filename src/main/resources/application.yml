spring:
  main:
    web-application-type: servlet
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: ifzz
    password: ifzz
    platform: postgres
  jpa:
    hibernate:
      ddl-auto: update
