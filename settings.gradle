pluginManagement {
    resolutionStrategy {
        eachPlugin {
        }
    }
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven { url 'https://dl.google.com/dl/android/maven2' }
    }
}
rootProject.name = "microservices"
