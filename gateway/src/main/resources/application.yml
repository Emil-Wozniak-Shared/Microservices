server:
  port: 8180
spring:
  application:
    name: gateway

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8010/eureka
management:
  cloudfoundry:
    enabled: true
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: "*"


